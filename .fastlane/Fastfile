lane :test_ios do
	scan(scheme: "Money", destination: "platform=iOS Simulator,OS=11.0,name=iPhone 8")
end

lane :test_mac do
	scan(scheme: "Money", destination: "platform=macOS")
end

lane :test_tvos do
	scan(scheme: "Money", destination: "platform=tvOS Simulator,OS=11.0,name=Apple TV")
end
